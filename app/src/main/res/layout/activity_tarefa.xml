<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".TarefaActivity"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/layTopo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/design_default_color_primary"
        android:paddingHorizontal="8dp"
        android:paddingTop="8dp"
        android:paddingBottom="20dp"
        >

    <TextView
        android:id="@+id/txtCategoria"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Categoria: "
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="@id/txtNome"
        android:layout_marginHorizontal="4dp"
        app:layout_constraintEnd_toStartOf="@id/cmdEditarTarefa"
        android:textColor="@color/white"
        />
    <ImageView
        android:id="@+id/chkTarefa"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:src="@drawable/ic_check_0"
        app:layout_constraintEnd_toStartOf="@id/txtNome"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/txtDescricao"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_margin="4dp"
        android:layout_marginEnd="8dp"
        android:tint="@color/white"
        />

    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/txtCategoria"
        app:layout_constraintStart_toEndOf="@id/chkTarefa"
        app:layout_constraintEnd_toStartOf="@id/cmdEditarTarefa"
        android:id="@+id/txtNome"
        android:textColor="@color/white"
        android:text="Tarefa"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_marginHorizontal="4dp"
        />

    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:id="@+id/txtDescricao"
        android:textColor="@color/white"
        app:layout_constraintTop_toBottomOf="@id/txtNome"
        app:layout_constraintStart_toStartOf="@id/txtNome"
        app:layout_constraintEnd_toStartOf="@id/cmdEditarTarefa"
        android:text="Descrição da Tarefa"
        android:lines="1"
        android:maxLines="1"
        android:layout_marginBottom="4dp"
        android:layout_marginHorizontal="4dp"

        />

    <ImageView
        android:id="@+id/cmdEditarTarefa"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:src="@drawable/ic_edit"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/txtDescricao"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_margin="4dp"
        android:tint="@color/white"
        />
    </androidx.constraintlayout.widget.ConstraintLayout>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvItems"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/layTopo"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_margin="4dp"
        />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/cmdNovoItem"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:src="@drawable/ic_add"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        />
</androidx.constraintlayout.widget.ConstraintLayout>